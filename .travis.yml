jobs:
  include:
    - stage: test_and_lint
      language: node_js
      node_js:
        - "10"
      cache:
        directories:
          - "$HOME/.npm"
      install:
        - npm ci
      script:
        - npm run lint
        - npm run test

    - stage: fastlane_android
      language: android
      jdk: oraclejdk8
      android:
        components:
          - tools
          - platform-tools
          - android-28
      before_install:
        - git clone https://$TEMP_GITHUB_TOKEN@github.com/iteam1337/egendata-private.git
        - nvm install 10.16.3
        - cd android && gem install bundler
        - bundle install
      script:
        - |
          VERSION=$(node -p "require('../package.json').version")
          echo $GOOGLE_PLAY | base64 --decode > ./google_play_store.json
          fastlane ci_internal version:$VERSION
          rm ./google_play_store.json

stages:
  - name: fastlane_android
    if: branch = master AND type != pull_request
  - name: test_and_lint
    if: branch = master AND type = pull_request
