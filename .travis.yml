jobs:
  include:
    - stage: test
      language: node_js
      node_js:
        - "10"
      cache:
        directories:
          - "$HOME/.npm"
      install:
        - npm ci
      script:
        - npm run lint
        - npm run test

    - stage: deploy
      language: node_js
      node_js:
        - "10"
      cache:
        directories:
          - "$HOME/.npm"
      install:
        - npm ci
      script:
        - npm run lint
        - npm run test

    - stage: android_fastlane
      language: android
      jdk: oraclejdk8
      android:
        components:
          - tools
          - platform-tools
          - android-28
          # - extra-google-m2repository
          # - extra-google-google_play_services
      before_install:
        # - nvm install 10.16.3
        - cd android && gem install bundler
        - bundle install
      # install: yarn
      script: fastlane travistest

stages:
  - android_fastlane
  - name: test
    if: branch = master AND type = pull_request
  - name: deploy
    if: branch = master AND type != pull_request
